
# PSEUDOCODE

for folder in DIR_TEXTS:
    checklist = check(ris_files) # ris_check_list
    if checklist['self']:
        sort_ref_info = make_sort_ref_info(folder.files, checklist)
    



    if rcl[0]:
        if self_file.md in DIR_OUTPUT:
            if '---ref_sort---' in self_file.md:
                read(ref_sort_data)
                update_metadata(self_file.md)
            else:
                with self_file.md open as f:
                    f.append(make_ref_sort_data())
        else:
            create(OUTPUT_DIR/self_file.md)
    if rcl[1]:
        pass
    if rcl[2]:
        pass


# ==============================================

"""


"""